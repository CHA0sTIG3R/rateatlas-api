app.ingest.api-key=test-api-key
prometheus.scrape.username=test-user
prometheus.scrape.password=test-password

# Disable real Redis in unit/slice tests
spring.data.redis.url=redis://localhost:6379
spring.cache.type=none
